services:
  app:
    build: .
    ports:
      - "8000:8000"
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: monmotdepasse
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
